{
  "name": "anirudh-interview-analytics-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Root package for GitHub Analytics Dashboard (frontend + backend)",
  "scripts": {
    "setup": "npm run install:all",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "start:backend": "cross-env PORT=5050 npm --prefix backend start",
    "start:frontend": "npm --prefix frontend start",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
